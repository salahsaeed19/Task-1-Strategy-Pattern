package shapes;

public class CircleAreaCalculationStrategy implements AreaCalculationStrategy {
    
    public double calculateArea(Shape shape) {
        
        return shape.getArea();
        
    }
    
}
